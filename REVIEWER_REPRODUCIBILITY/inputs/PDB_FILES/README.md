# External full corpus placeholder

Do not put a partial corpus here and treat it as a full release. For a full reconstruction, supply the complete frozen `PDB_FILES` hierarchy externally and pass its path with `--source`. The distributed fixture CIFs live separately under `fixture/PDB_FILES`.
