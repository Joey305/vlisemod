# Optional website-compatibility augmentation

These scripts are outside scientific reproduction. `reproduce.py --web-compat` first copies a completed scientific database and only then creates website-facing synonym/diagram materializations. No compatibility table is required by the Stage-15 scientific release validator.
